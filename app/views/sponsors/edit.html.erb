<%= link_to tr('show'), @sponsor %> :: <%= link_to tr('home'), {controller: :welcome, action: :index} %>

<h1><%= tr('sponsor') %></h1>

<%= render 'form' %>
<% if @user && @user.can_delete_sponsor_record? %>
<div class="delete-record"><%= link_to tr('destroy'), @sponsor, method: :delete, data: { confirm: tr('confirm_destroy') } %></div>
<% end %>
